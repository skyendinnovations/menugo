import { Stack } from 'expo-router';

export default function TablesLayout() {
  return (
    <Stack
      screenOptions={{
        headerStyle: { backgroundColor: '#000' },
        headerTintColor: '#fff',
      }}
    />
  );
}
