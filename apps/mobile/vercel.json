{
  "installCommand": "cd ../.. && bun install && node scripts/hoist-bun-deps.cjs",
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
