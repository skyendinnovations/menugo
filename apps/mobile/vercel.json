{
  "buildCommand": "cd ../.. && corepack enable && pnpm install --shamefully-hoist && cd apps/mobile && npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "echo 'skip'"
}
